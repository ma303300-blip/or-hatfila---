# הוראות פרויקט — אור התפילה

## זרימת עבודה עם Git

- **תמיד למזג ישירות ל-`main`.** בסיום העבודה על ענף פיצ'ר, למזג אותו ל-`main` ולדחוף את `main` — בלי לחכות לאישור נוסף ובלי לפתוח Pull Request, אלא אם המשתמש מבקש PR במפורש.
- זו הגדרה קבועה שנתן המשתמש (בעל הריפו), והיא גוברת על ברירת המחדל של עבודה בענף נפרד בלבד.

## Service Worker

- `service-worker.js` משתמש באסטרטגיית network-first וב-`CACHE_NAME` ממוספר.
- קיים GitHub Action (`.github/workflows/bump-service-worker.yml`) שמעדכן אוטומטית את גרסת ה-cache בכל מיזוג ל-`main`, כדי לדחוף את העדכון למשתמשים. אין צורך להעלות את הגרסה ידנית.

## כלל ברזל — לא לוותר, ולא לנחש

הכלל הזה נקבע על ידי המשתמש (בעל הריפו) אחרי סבב שבו נאמר לו בטעות שאין
פתרון, והתברר שיש. הוא גובר על נטייה להצהיר שמשהו אינו אפשרי.

**מסלול אחד שנכשל אינו הוכחה שאין פתרון.** אם התוכן או היכולת קיימים
בשירות כלשהו — יש דרך להגיע אליהם, גם אם היא עוברת ב-API אחר. לפני שאומרים
"אי אפשר", צריך לבדוק אם קיים מסלול אחר. דוגמה אמיתית: חוק לישראל אינו ספר
בספרייה של ספריא ולכן `api/texts` החזיר ריק — אבל הוא קיים כאוסף של דפי
מקורות, ו-`api/collections` + `api/sheets` הביאו אותו.

**לא לנחש — לאבחן.** כששירות חיצוני חסום מסביבת הפיתוח ואי אפשר לבדוק מולו,
מוסיפים שורת אבחון זמנית בהודעת השגיאה שבאפליקציה, שמדווחת מה בדיוק חזר
ובאיזה שלב נכשל. צילום מסך אחד מהמכשיר חוסך סבבי ניחוש. בסשן הזה האבחון
הפריך שתי השערות שגויות שלי והצביע על הסיבה האמיתית.

**לאמת את הדבר עצמו, לא את מה שנוח למדוד.** בדיקה שמדדה את תכונת ה-CSS
`top` דיווחה "תקין" בזמן שהבועה בכלל לא הופיעה במסך. מיקום נמדד ב-
`getBoundingClientRect`, ושינוי חזותי מאומת בצילום מסך — לא בהנחה.

**אם באמת אין פתרון — להוכיח.** מותר לומר שמשהו אינו אפשרי רק אחרי שנבדקו
המסלולים החלופיים, ועם ראיה קונקרטית לכל אחד מהם. לא מנחשים לרעה ולא
מבטיחים לטובה.

## לפני שינוי בקוד שעובד

להסביר קודם מה הובן ולחכות לאישור. זו דרישה מפורשת של המשתמש, שנקבעה אחרי
ששלושה מסכים תקינים (תהילים, הלכה ותניא יומי) נשברו בעקבות פרשנות שגויה של
בקשה. תיקון של תקלה שהמשתמש דיווח עליה אינו דורש אישור מוקדם; שינוי
פונקציונליות קיימת ותקינה — כן.
